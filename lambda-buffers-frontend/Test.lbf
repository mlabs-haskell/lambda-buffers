module
  Test

import A
import qualified B
import G.A
import qualified G.A
import qualified C (A,B)
import qualified C as D (A,B)
import qualified C as D ()

sum
  Maybe
    a =
      Just a
      |
      Nothing

sum Either a b = Left a | Right b

sum List a = Nil | List a (List a)

opaque Int a